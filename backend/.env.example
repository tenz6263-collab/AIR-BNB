PORT=4000
# Leave MONGODB_URI empty to run against the bundled in-memory dataset.
MONGODB_URI=mongodb://127.0.0.1:27017/airbnb_clone
# Comma separated allowed browser origins (full origin or bare host).
# localhost is always allowed. Leave empty to allow every origin.
CLIENT_ORIGIN=http://localhost:5173
