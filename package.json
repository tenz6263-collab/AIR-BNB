{
  "name": "airbnb-listing-clone",
  "version": "1.0.0",
  "private": true,
  "description": "Pixel-perfect Airbnb listing page clone - MERN stack (Playpower Labs assignment)",
  "author": "Rishi Detroja",
  "scripts": {
    "postinstall": "node scripts/install-all.js",
    "dev": "concurrently -n api,web -c blue,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend run start",
    "seed": "npm --prefix backend run seed",
    "test": "npm test --prefix backend && npm test --prefix frontend",
    "e2e": "node scripts/e2e.js",
    "snapshot": "node scripts/snapshot.js",
    "package": "node scripts/package.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "puppeteer-core": "^25.10.0"
  }
}
